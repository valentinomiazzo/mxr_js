/*global require */

require.config({
    baseUrl: "js/src",
    config: {
    },
    //REMEMBER: keep it in sync with bower.json
    paths: {
        "In": "../../bower_components/in_js/js/src/In"
    },
    packages: [

    ]
});